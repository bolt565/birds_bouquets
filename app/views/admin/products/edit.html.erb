<% content_for :title, "Edit #{@product.name} — Admin" %>

<div class="max-w-3xl">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit: <%= @product.name %></h1>
  <%= render "form", product: @product %>
</div>
