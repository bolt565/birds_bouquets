<% content_for :title, "Edit #{@category.name} — Admin" %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit: <%= @category.name %></h1>
  <%= render "form", category: @category %>
</div>
